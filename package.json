{
  "name": "freedom-estimating-system",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24.0.0" },
  "scripts": {
    "build": "esbuild client/app.js --bundle --format=iife --loader:.png=file --outfile=dist/app.js",
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node --test tests/*.test.js",
    "test:browser": "playwright test",
    "password": "node scripts/password.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "ws": "^8.18.3",
    "yjs": "^13.6.27"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "esbuild": "^0.25.9"
  }
}
